/**
 * #52 — Same as #5. validatePattern normalizes internally only; raw pattern
 *       remains in PathPart. Already covered by `verify/05-anchor-drift.ts`.
 */
console.log('VERDICT: DUP-#5 — see verify/05-anchor-drift.ts');
